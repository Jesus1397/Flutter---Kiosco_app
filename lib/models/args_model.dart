import 'package:flutter/material.dart';

class ArgModel {
  String id;
  String name;
  String colName;

  ArgModel({
    @required this.id,
    @required this.name,
    @required this.colName,
  });
}
